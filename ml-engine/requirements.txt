# SMART-Mataram ML Engine — dependencies
# Python 3.11+ (teruji di 3.11 VPS & 3.14 lokal).
# Pin longgar (>=) agar wheel terbaru kepakai lintas versi Python.

# ── Sprint 1 (sync gangguan) ──────────────────────────────────────────────────
supabase>=2.9
requests>=2.31
python-dotenv>=1.0

# ── Sprint 2+ (cuaca, fitur, model) ──────────────────────────────────────────
# pandas>=2.2
# pyyaml>=6.0
scikit-learn>=1.5
xgboost>=2.1
